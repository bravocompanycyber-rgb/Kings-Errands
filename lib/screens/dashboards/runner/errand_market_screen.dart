import 'package:flutter/material.dart';

class ErrandMarketScreen extends StatelessWidget {
  const ErrandMarketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Errand Market')),
      body: const Center(
        child: Text('Errand Market Screen'),
      ),
    );
  }
}
